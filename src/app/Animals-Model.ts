import { Position } from "./position";

export class Animals {
    id: number;
    name: string;
    heartRate: number;
    location: Position;
    prevLocations: Position[];
    showPath:boolean= false;

    
}
